export type User = {
  id: string,
  name: string,
  email: string
}

export type SignupFormData = {
  name: string,
  email: string,
  password: string
}

export type LoginCredentials = {
  email: string,
  password: string
}